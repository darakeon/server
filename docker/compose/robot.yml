version: "3.3"
services:
  robot-${TASK}:
    image: "darakeon/dfm-robot"
    container_name: robot-${TASK}
    volumes:
      - "/var/cfg/dfm:/var/cfg"
      - "/var/data/dfm/robot:/var/robot/data"
      - "/var/data/dfm/nh:/var/logs/dfm/nh"
    environment:
      - TASK=${TASK}
